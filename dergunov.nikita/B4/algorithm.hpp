#ifndef B4_ALGORITHM_HPP
#define B4_ALGORITHM_HPP

#include <vector>
#include <iostream>

void executeAlgorithm(std::istream &is = std::cin, std::ostream &os = std::cout);

struct DataStruct;
using DataStructVector = std::vector<DataStruct>;

#endif
